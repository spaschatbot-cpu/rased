node_modules
dist
dist-ssr
*.local

# local backend store — the dev fallback for the hosted database
.data
.vercel
.env
.env.*
!.env.example
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea
*.log

# personal Claude Code permissions — grants delete power, not a team default
.claude/settings.local.json

# credentials for scripts/reset-test-data.mjs — never the app
.env.admin
